# Hero: back to 16:9
**Date:** 2026-08-21 · **Status:** done

## Goal
Tina: "nevermind lets o baack to 16:9" — undo the 21:9 crop from the
previous change.

## What changed
- `hero-home-4.jpg` (5504×3072, ≈1.792:1 — already almost 16:9) cropped to
  an exact 16:9: centred, ~21px trimmed off each side, 5461×3072, verified
  ratio 1.7777. Saved as `public/hero-home-6.jpg`, registered in
  `scripts/optimise-images.mjs`'s `JOBS` array, variants generated.
- `app/page.tsx`: hero `<img>` repointed at `hero-home-6-*.webp`.
- The inverse-spotlight overlay from the previous change (dark around the
  edges, near-transparent on the models) is kept, but recentred for this
  crop's own proportions: it's taller than the 21:9 version (3072 vs 2359,
  same models at the same absolute pixel position, now with the marble
  floor visible again below them), so the models occupy a smaller share of
  the frame vertically. Moved the radial's vertical centre 48%→40% and its
  ellipse height 90%→70% so the bright zone stays sized to the models
  instead of spreading down into the now-visible floor. Horizontal centre
  (68%) didn't need to move — the crop to reach 16:9 only trimmed ~21px off
  each side.

## Verification
- `npx tsc --noEmit` — clean.
- `npx eslint app/page.tsx scripts/optimise-images.mjs` — clean.
- Playwright screenshots at 1999×900 and 390×844 (mobile): 16:9 frame, both
  models and chandeliers back in view, spotlight still centred on the
  models rather than drifting onto the floor.

## Notes / follow-ups
- `hero-home-5.jpg` (the 21:9 crop) is left in place, unreferenced, same
  "cheap to keep, reversible" call as every earlier hero file this week.
