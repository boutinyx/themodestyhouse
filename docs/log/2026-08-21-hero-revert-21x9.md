# Revert the 21:9 crop — back to hero-home-6.jpg with the objectPosition/scale hack
**Date:** 2026-08-21 · **Status:** done

## Goal
Tina, minutes after the 21:9 crop landed: "revert i never midn the 21:9
[screenshot] i just wanted it to be like this." The screenshot she pointed
at was her OWN live site mid-session — the previous `hero-home-6.jpg` +
`objectPosition: 'center 100%'` + `transform: scale(1.18)` setup, not the
21:9 crop that later replaced it. The "cut the top off to make it 21:9" ask
had been a request for a look, not literally that aspect ratio.

## What changed
- `app/page.tsx` — hero `<img>` reverted to `hero-home-6-*.webp` with its
  `srcSet`, and `style={{ objectPosition: 'center 100%', transform:
  'scale(1.18)', transformOrigin: '70% 35%' }}` restored exactly as it was
  before the 21:9 detour.
- `scripts/optimise-images.mjs` — updated `hero-home-8.jpg`'s job comment to
  note it landed and was reverted the same day; left the job itself
  registered (the crop is still correct craft, just not what she wanted
  used).

## Verification
- `npx tsc --noEmit` — clean.
- `npx eslint app/page.tsx scripts/optimise-images.mjs` — clean.
- `npx vitest run --exclude ".claude/**"` — 722/722 passing, 44/44 files.
- Screenshotted at 1999×900 — matches the composition from the previous
  ("move the women up" / "zoom in") revisions exactly.

## Notes / follow-ups
- `hero-home-8.jpg` (the 21:9 crop) stays on disk, registered, generated,
  unreferenced — same "cheap to keep" call as every other superseded hero
  file this week, in case it's wanted again later.
